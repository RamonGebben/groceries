<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :shop, :placeholder => 'Which shop?' %>
  </div>
  <div class="field">
    <%= f.text_field :title, :placeholder => 'What do you need?' %>
  </div>
  <div class="field">
    <%= f.text_field :brand, :placeholder => 'Which brand?' %>
  </div>
  <div class="field">
    <%= f.number_field :quantity, :placeholder => 'How many?' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
